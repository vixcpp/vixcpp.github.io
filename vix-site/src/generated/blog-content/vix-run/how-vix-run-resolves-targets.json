{"html":"<h1>How vix run Resolves Targets</h1>\n<p><code>vix run</code> is the command that turns a source file or project into something executable, then runs it.</p>\n<p>It looks simple from the outside:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span></span></code></pre><p>or:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#9ECBFF\"> main.cpp</span></span></code></pre><p>But internally, <code>vix run</code> has to answer several questions:</p>\n<pre class=\"blog-code blog-code--plain\"><code>Is this a single C++ file?\nIs this a project?\nIs the project using CMakeLists.txt?\nIs the project using vix.app?\nWhat target should be built?\nWhere is the executable?\nWhich arguments belong to Vix?\nWhich arguments belong to the program?</code></pre><p>Target resolution is the part of <code>vix run</code> that answers these questions.</p>\n<h2>Two main modes</h2>\n<p><code>vix run</code> has two major modes:</p>\n<pre class=\"blog-code blog-code--plain\"><code>script mode\nproject mode</code></pre><p>Script mode runs a single C++ file.</p>\n<p>Project mode runs a CMake or <code>vix.app</code> project.</p>\n<p>Example script mode:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#9ECBFF\"> main.cpp</span></span></code></pre><p>Example project mode:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span></span></code></pre><p>from inside a project directory.</p>\n<h2>Script mode</h2>\n<p>Script mode is used when the input is a C++ source file.</p>\n<p>Example:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#9ECBFF\"> main.cpp</span></span></code></pre><p>In this mode, Vix treats the file as the entry point.</p>\n<p>The target identity comes from the source file.</p>\n<p>Conceptually:</p>\n<pre class=\"blog-code blog-code--plain\"><code>input file: main.cpp\nscript target: main</code></pre><p>Vix can compile the file, link the required libraries, then execute the produced binary.</p>\n<h2>Project mode</h2>\n<p>Project mode is used when Vix detects a project root.</p>\n<p>A project root contains one of these files:</p>\n<pre class=\"blog-code blog-code--plain\"><code>CMakeLists.txt\nvix.app</code></pre><p>Resolution order:</p>\n<pre class=\"blog-code blog-code--plain\"><code>1. CMakeLists.txt\n2. vix.app</code></pre><p>If a <code>CMakeLists.txt</code> exists, Vix uses the CMake project.</p>\n<p>If no <code>CMakeLists.txt</code> exists but <code>vix.app</code> exists, Vix uses the <code>vix.app</code> project.</p>\n<h2>Why target resolution matters</h2>\n<p>Running a project is not only about building.</p>\n<p>After building, Vix needs to find the executable.</p>\n<p>That requires a target name.</p>\n<p>For example:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = hello\ntype = executable\nstandard = c++20\n\nsources = [\n  src/main.cpp,\n]</code></pre><p>The executable target is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello</code></pre><p>So <code>vix run</code> should build and search for:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello</code></pre><p>not necessarily the folder name.</p>\n<h2>The old simple assumption</h2>\n<p>For many CMake projects, Vix can guess the target from the project folder.</p>\n<p>Example:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello/\n  CMakeLists.txt\n  src/\n    main.cpp</code></pre><p>Folder name:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello</code></pre><p>Target name:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello</code></pre><p>In this case, guessing from the folder works.</p>\n<p>But this assumption breaks when the target name differs from the directory name.</p>\n<h2>vix.app makes target identity explicit</h2>\n<p>With <code>vix.app</code>, the manifest declares the target name directly:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = server</code></pre><p>The folder could be named:</p>\n<pre class=\"blog-code blog-code--plain\"><code>backend-app/</code></pre><p>But the target is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>server</code></pre><p>So <code>vix run</code> must use the resolved target name from the manifest.</p>\n<p>It should not guess from:</p>\n<pre class=\"blog-code blog-code--plain\"><code>project directory name\ngenerated CMake directory name</code></pre><h2>Project resolution result</h2>\n<p>A good project resolver returns enough metadata for <code>vix run</code>.</p>\n<p>For a CMake project:</p>\n<pre class=\"blog-code blog-code--plain\"><code>kind            = CMake\nuserProjectDir  = project/\ncmakeSourceDir  = project/\ncmakeListsPath  = project/CMakeLists.txt\ntargetName      = project folder name or resolved target\ngenerated       = false</code></pre><p>For a <code>vix.app</code> project:</p>\n<pre class=\"blog-code blog-code--plain\"><code>kind            = vix.app\nuserProjectDir  = project/\ncmakeSourceDir  = project/.vix/generated/app/\ncmakeListsPath  = project/.vix/generated/app/CMakeLists.txt\nappManifestPath = project/vix.app\ntargetName      = manifest.name\ngenerated       = true</code></pre><p><code>vix run</code> should use this resolved result instead of duplicating project detection logic.</p>\n<h2>userProjectDir</h2>\n<p><code>userProjectDir</code> is the real project root.</p>\n<p>It contains:</p>\n<pre class=\"blog-code blog-code--plain\"><code>CMakeLists.txt</code></pre><p>or:</p>\n<pre class=\"blog-code blog-code--plain\"><code>vix.app\nsrc/\ninclude/\nassets/</code></pre><p>For <code>vix.app</code>, this remains the user project root even though the generated CMake file lives elsewhere.</p>\n<p><code>vix run</code> should use <code>userProjectDir</code> for:</p>\n<pre class=\"blog-code blog-code--plain\"><code>working directory decisions\nwatch mode\nproject-relative operations\ncalling vix build\nuser-facing diagnostics</code></pre><h2>cmakeSourceDir</h2>\n<p><code>cmakeSourceDir</code> is the directory passed to CMake configure.</p>\n<p>For CMake projects:</p>\n<pre class=\"blog-code blog-code--plain\"><code>cmakeSourceDir = project/</code></pre><p>For <code>vix.app</code> projects:</p>\n<pre class=\"blog-code blog-code--plain\"><code>cmakeSourceDir = project/.vix/generated/app/</code></pre><p><code>vix run</code> usually should not treat this as the user project root.</p>\n<p>It is only the active CMake source directory.</p>\n<h2>targetName</h2>\n<p><code>targetName</code> is the target to build and run.</p>\n<p>For <code>vix.app</code>, this comes from:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = ...</code></pre><p>Example:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = api_server</code></pre><p>Then <code>vix run</code> should build:</p>\n<pre class=\"blog-code blog-code--plain\"><code>api_server</code></pre><p>and search for an executable named:</p>\n<pre class=\"blog-code blog-code--plain\"><code>api_server</code></pre><p>or platform equivalent:</p>\n<pre class=\"blog-code blog-code--plain\"><code>api_server.exe</code></pre><p>on Windows.</p>\n<h2>generated</h2>\n<p>The <code>generated</code> flag tells Vix whether the active CMake project came from <code>vix.app</code>.</p>\n<p>This helps diagnostics.</p>\n<p>For example, <code>vix run</code> can show different context:</p>\n<pre class=\"blog-code blog-code--plain\"><code>Project directory:\n  app/\n\nGenerated CMake source:\n  app/.vix/generated/app/</code></pre><p>It also helps avoid using generated directories for user-facing operations.</p>\n<h2>Running a CMake project</h2>\n<p>For a normal CMake project:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello/\n  CMakeLists.txt\n  src/\n    main.cpp</code></pre><p><code>vix run</code> can do:</p>\n<pre class=\"blog-code blog-code--plain\"><code>resolve project\nbuild target\nfind executable\nrun executable</code></pre><p>Conceptually:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> build</span><span style=\"color:#79B8FF\"> --dir</span><span style=\"color:#9ECBFF\"> hello</span></span>\n<span class=\"line\"><span style=\"color:#B392F0\">./build-ninja/hello</span></span></code></pre><p>The actual implementation may call internal build functions or invoke the build pipeline directly, but the logic is the same.</p>\n<h2>Running a vix.app project</h2>\n<p>For a <code>vix.app</code> project:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello/\n  vix.app\n  src/\n    main.cpp</code></pre><p>with:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = hello\ntype = executable\nstandard = c++20\n\nsources = [\n  src/main.cpp,\n]</code></pre><p><code>vix run</code> should do:</p>\n<pre class=\"blog-code blog-code--plain\"><code>resolve vix.app\ngenerate internal CMake if needed\nbuild target hello\nfind executable hello\nrun executable</code></pre><p>The generated CMake directory should not become the user project root.</p>\n<h2>Build before run</h2>\n<p><code>vix run</code> should ensure the target is built before execution.</p>\n<p>Conceptually:</p>\n<pre class=\"blog-code blog-code--plain\"><code>vix run\n  -&gt; resolve project\n  -&gt; build selected target\n  -&gt; resolve executable path\n  -&gt; execute binary</code></pre><p>This is why <code>vix run</code> depends on correct <code>vix build</code> planning.</p>\n<p>If <code>vix build</code> builds the wrong target, <code>vix run</code> will search for the wrong executable.</p>\n<h2>Executable lookup</h2>\n<p>After building, <code>vix run</code> needs to find the executable.</p>\n<p>Common locations include:</p>\n<pre class=\"blog-code blog-code--plain\"><code>build-ninja/&lt;target&gt;\nbuild-ninja/bin/&lt;target&gt;\nbuild-ninja/src/&lt;target&gt;</code></pre><p>For Windows, the executable may include:</p>\n<pre class=\"blog-code blog-code--plain\"><code>.exe</code></pre><p>So for target:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello</code></pre><p>Vix may search for:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello\nhello.exe</code></pre><p>depending on platform.</p>\n<h2>output_dir and executable lookup</h2>\n<p><code>vix.app</code> supports:</p>\n<pre class=\"blog-code blog-code--plain\"><code>output_dir = bin</code></pre><p>This means the executable may be under:</p>\n<pre class=\"blog-code blog-code--plain\"><code>build-ninja/bin/</code></pre><p>Example:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = server\ntype = executable\nstandard = c++20\noutput_dir = bin\n\nsources = [\n  src/main.cpp,\n]</code></pre><p>After build, Vix should search:</p>\n<pre class=\"blog-code blog-code--plain\"><code>build-ninja/bin/server</code></pre><p>as well as other common locations.</p>\n<h2>Why recursive fallback exists</h2>\n<p>Some build systems place outputs in less obvious directories.</p>\n<p>A fallback recursive search can help when the executable is not in the expected locations.</p>\n<p>However, recursive search should still be guided by:</p>\n<pre class=\"blog-code blog-code--plain\"><code>targetName</code></pre><p>It should not run a random executable just because it exists.</p>\n<p>Target name keeps the search safe and predictable.</p>\n<h2>Running libraries</h2>\n<p><code>vix run</code> is mainly for executable targets.</p>\n<p>If a project is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = mathlib\ntype = static</code></pre><p>there may be no executable to run.</p>\n<p>The correct command is:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> build</span></span></code></pre><p>To run something for a library, use a test or example executable:</p>\n<pre class=\"blog-code blog-code--plain\"><code>tests/vix.app\nexamples/basic/vix.app</code></pre><p>Then run:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#79B8FF\">cd</span><span style=\"color:#9ECBFF\"> tests</span></span>\n<span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span></span></code></pre><p>or:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#79B8FF\">cd</span><span style=\"color:#9ECBFF\"> examples/basic</span></span>\n<span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span></span></code></pre><h2>Runtime arguments</h2>\n<p><code>vix run</code> must separate Vix arguments from program arguments.</p>\n<p>For example:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#9ECBFF\"> main.cpp</span><span style=\"color:#79B8FF\"> --run</span><span style=\"color:#79B8FF\"> --name</span><span style=\"color:#9ECBFF\"> Gaspard</span></span></code></pre><p>or for project mode:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#79B8FF\"> --</span><span style=\"color:#79B8FF\"> --name</span><span style=\"color:#9ECBFF\"> Gaspard</span></span></code></pre><p>The exact boundary depends on the command design, but the principle is important:</p>\n<pre class=\"blog-code blog-code--plain\"><code>arguments before the boundary configure Vix\narguments after the boundary go to the program</code></pre><p>Without a clear boundary, Vix may consume arguments intended for the user program.</p>\n<h2>Script mode target resolution</h2>\n<p>In script mode:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#9ECBFF\"> main.cpp</span></span></code></pre><p>Vix can derive the target name from the file:</p>\n<pre class=\"blog-code blog-code--plain\"><code>main</code></pre><p>The compiled output can be stored in a Vix build/cache directory.</p>\n<p>The exact path is an implementation detail.</p>\n<p>What matters is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>the source file is the entry point\nthe output is executable\nthe program receives runtime arguments</code></pre><h2>Project mode target resolution</h2>\n<p>In project mode:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span></span></code></pre><p>Vix must resolve a project first.</p>\n<p>Then it uses the project target.</p>\n<p>For CMake:</p>\n<pre class=\"blog-code blog-code--plain\"><code>targetName = project default target</code></pre><p>For <code>vix.app</code>:</p>\n<pre class=\"blog-code blog-code--plain\"><code>targetName = manifest.name</code></pre><p>This is the main difference.</p>\n<h2>Explicit build target</h2>\n<p>A user may provide an explicit build target.</p>\n<p>Example:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span><span style=\"color:#79B8FF\"> --build-target</span><span style=\"color:#9ECBFF\"> server</span></span></code></pre><p>In that case, the explicit target should take priority over the default target.</p>\n<p>Resolution rule:</p>\n<pre class=\"blog-code blog-code--plain\"><code>if --build-target is provided:\n  use --build-target\nelse:\n  use resolved targetName</code></pre><p>This is useful for CMake projects with multiple targets.</p>\n<p>For <code>vix.app</code>, there is normally one target, so the manifest name is usually enough.</p>\n<h2>Watch mode</h2>\n<p>Watch mode should watch the real project directory.</p>\n<p>For CMake projects:</p>\n<pre class=\"blog-code blog-code--plain\"><code>watch project/</code></pre><p>For <code>vix.app</code> projects:</p>\n<pre class=\"blog-code blog-code--plain\"><code>watch project/</code></pre><p>It should not watch only:</p>\n<pre class=\"blog-code blog-code--plain\"><code>project/.vix/generated/app/</code></pre><p>because that directory contains generated files, not the user’s real source code.</p>\n<p>For <code>vix.app</code>, watch mode should observe:</p>\n<pre class=\"blog-code blog-code--plain\"><code>vix.app\nsrc/\ninclude/\nassets/</code></pre><h2>Generated CMake and run</h2>\n<p>For a <code>vix.app</code> project, generated CMake is part of the build process.</p>\n<p>But <code>vix run</code> should still think in terms of the user project.</p>\n<p>The important values are:</p>\n<pre class=\"blog-code blog-code--plain\"><code>userProjectDir  -&gt; where the project lives\ncmakeSourceDir  -&gt; where generated CMake lives\ntargetName      -&gt; what to build and run</code></pre><p>This keeps <code>vix run</code> aligned with <code>vix build</code>.</p>\n<h2>CMake presets</h2>\n<p>For normal CMake projects, <code>vix run</code> may use CMake presets when available.</p>\n<p>For <code>vix.app</code>, presets are less relevant because the CMake project is generated internally.</p>\n<p>The important rule is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>normal CMake project -&gt; can use project CMake presets\nvix.app project      -&gt; use generated CMake and Vix build planning</code></pre><h2>Error: project not found</h2>\n<p>If Vix cannot find:</p>\n<pre class=\"blog-code blog-code--plain\"><code>CMakeLists.txt</code></pre><p>or:</p>\n<pre class=\"blog-code blog-code--plain\"><code>vix.app</code></pre><p>project mode cannot run.</p>\n<p>Example error:</p>\n<pre class=\"blog-code blog-code--plain\"><code>Unable to determine the project directory.\nMissing CMakeLists.txt or vix.app.</code></pre><p>Fix:</p>\n<pre class=\"blog-code blog-code--plain\"><code>run vix from a project directory\nor create vix.app\nor create CMakeLists.txt</code></pre><h2>Error: executable not found</h2>\n<p>This can happen when:</p>\n<pre class=\"blog-code blog-code--plain\"><code>the target is a library\nthe build failed\nthe target name is wrong\noutput_dir is unexpected\nthe executable was not produced</code></pre><p>For <code>vix.app</code>, check:</p>\n<pre class=\"blog-code blog-code--plain\"><code>name = ...\ntype = executable\noutput_dir = ...</code></pre><p>If the type is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>type = static</code></pre><p>then <code>vix run</code> has no executable to run.</p>\n<h2>Error: wrong target name</h2>\n<p>This can happen if the build system guesses from the folder name instead of using the manifest name.</p>\n<p>Example:</p>\n<pre class=\"blog-code blog-code--plain\"><code>folder: hello-project\nmanifest name: hello</code></pre><p>Correct target:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello</code></pre><p>Wrong target:</p>\n<pre class=\"blog-code blog-code--plain\"><code>hello-project</code></pre><p>The fix is to use the resolved target name from project resolution.</p>\n<h2>Error: generated directory used as project root</h2>\n<p>For <code>vix.app</code>, this is wrong:</p>\n<pre class=\"blog-code blog-code--plain\"><code>project root = .vix/generated/app</code></pre><p>That can break:</p>\n<pre class=\"blog-code blog-code--plain\"><code>watch mode\nruntime working directory\ntarget guessing\nexports\ndiagnostics</code></pre><p>The project root should be:</p>\n<pre class=\"blog-code blog-code--plain\"><code>userProjectDir</code></pre><p>The generated directory should be:</p>\n<pre class=\"blog-code blog-code--plain\"><code>cmakeSourceDir</code></pre><h2>Runtime working directory</h2>\n<p>The runtime working directory affects relative file access.</p>\n<p>If the program opens:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">std</span><span style=\"color:#E1E4E8\">::</span><span style=\"color:#B392F0\">ifstream</span><span style=\"color:#B392F0\"> file</span><span style=\"color:#E1E4E8\">(</span><span style=\"color:#9ECBFF\">\"config/app.json\"</span><span style=\"color:#E1E4E8\">);</span></span></code></pre><p>then it depends on where the process starts.</p>\n<p>For apps with resources, this matters.</p>\n<p>A <code>vix.app</code> project may copy resources next to the executable:</p>\n<pre class=\"blog-code blog-code--plain\"><code>build-ninja/bin/config/app.json</code></pre><p>but the program may run from the project root unless Vix changes the working directory.</p>\n<p>This is a design choice <code>vix run</code> should make deliberately.</p>\n<h2>Running with resources</h2>\n<p>If the manifest says:</p>\n<pre class=\"blog-code blog-code--plain\"><code>output_dir = bin\n\nresources = [\n  &quot;data/config.json=config/config.json&quot;,\n]</code></pre><p>then the resource may be copied to:</p>\n<pre class=\"blog-code blog-code--plain\"><code>build-ninja/bin/config/config.json</code></pre><p><code>vix run</code> should either:</p>\n<pre class=\"blog-code blog-code--plain\"><code>run from the executable directory</code></pre><p>or document the runtime working directory clearly.</p>\n<p>The important point is predictability.</p>\n<h2>Project mode should not duplicate build logic</h2>\n<p><code>vix run</code> should not reinvent <code>vix build</code>.</p>\n<p>It should reuse the same project resolution and build planning rules.</p>\n<p>The flow should be:</p>\n<pre class=\"blog-code blog-code--plain\"><code>resolve project\ncall build pipeline with target\nresolve executable\nrun executable</code></pre><p>If <code>vix run</code> and <code>vix build</code> resolve projects differently, bugs appear.</p>\n<p>Especially with <code>vix.app</code>.</p>\n<h2>Shared resolver</h2>\n<p>A shared resolver should answer:</p>\n<pre class=\"blog-code blog-code--plain\"><code>kind\nuserProjectDir\ncmakeSourceDir\ntargetName\ngenerated\ncmakeListsPath\nappManifestPath</code></pre><p>Both <code>vix build</code> and <code>vix run</code> should use this same model.</p>\n<p>That keeps behavior consistent.</p>\n<h2>Good output</h2>\n<p>When verbose mode is enabled, <code>vix run</code> can show:</p>\n<pre class=\"blog-code blog-code--plain\"><code>Project:\n  kind: vix.app\n  root: /path/to/project\n  generated CMake: /path/to/project/.vix/generated/app\n  target: hello\n\nBuild:\n  preset: dev-ninja\n  directory: build-ninja\n\nRun:\n  executable: build-ninja/bin/hello</code></pre><p>This makes target resolution visible.</p>\n<h2>Why this matters</h2>\n<p>Most command-line tools feel simple because the hard decisions are hidden.</p>\n<p>But hidden decisions still need to be correct.</p>\n<p><code>vix run</code> should feel like:</p>\n<pre class=\"blog-code shiki github-dark\" style=\"background-color:#24292e;color:#e1e4e8\" tabindex=\"0\"><code><span class=\"line\"><span style=\"color:#B392F0\">vix</span><span style=\"color:#9ECBFF\"> run</span></span></code></pre><p>But internally it must correctly handle:</p>\n<pre class=\"blog-code blog-code--plain\"><code>single-file scripts\nCMake projects\nvix.app projects\ngenerated CMake\noutput directories\nruntime arguments\nwatch mode</code></pre><p>That is why target resolution matters.</p>\n<h2>Future native vix.app builds</h2>\n<p>In the future, <code>vix.app</code> may build natively:</p>\n<pre class=\"blog-code blog-code--plain\"><code>vix.app\n  -&gt; BuildGraph\n  -&gt; ObjectCache\n  -&gt; Scheduler\n  -&gt; Link</code></pre><p><code>vix run</code> should not need a major redesign for that.</p>\n<p>If the resolver still provides:</p>\n<pre class=\"blog-code blog-code--plain\"><code>targetName\nbuildDir\noutput path\nproject root</code></pre><p>then the execution step stays similar.</p>\n<p>Only the build engine changes underneath.</p>\n<h2>Conclusion</h2>\n<p><code>vix run</code> resolves targets by first deciding whether it is running a single file or a project.</p>\n<p>For projects, it resolves either:</p>\n<pre class=\"blog-code blog-code--plain\"><code>CMakeLists.txt</code></pre><p>or:</p>\n<pre class=\"blog-code blog-code--plain\"><code>vix.app</code></pre><p>For <code>vix.app</code>, the manifest name is the target name.</p>\n<p>That target name must drive both the build step and the executable lookup.</p>\n<p>The key rule is:</p>\n<pre class=\"blog-code blog-code--plain\"><code>do not guess from the generated directory\ndo not guess from the folder when the manifest gives a name</code></pre><p>A clean <code>vix run</code> model uses shared project resolution, builds the correct target, finds the executable, and runs it with a clear argument boundary.</p>\n"}
