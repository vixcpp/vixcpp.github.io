::: warning Hardware requirements
Vix.cpp runs everywhere, but performance depends on your hardware.

For real-world production workloads, use a modern multi-core CPU, fast I/O,
and enough memory to avoid contention. GPU acceleration is optional and only
required for specialized compute workloads.
:::
